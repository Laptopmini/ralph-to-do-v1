# PRD: To-Do UI, Styling, and Wiring

## Tasks

- [x] Create app.js ES module. Create `src/app.js` as an ES module with named exports — import `{ loadTodos, saveTodos }` from `./storage.js` and `{ addTodo, toggleTodo, deleteTodo }` from `./todos.js`, initialize state on DOMContentLoaded, expose render() function that queries todo-list and renders li items with checkbox/text/delete buttons using data-testid attributes, updates todo-count with active count, toggles empty-state visibility, applies filter state; wire form submit handler to add todos, delegated click handlers for toggle/delete with 200ms leave animation via is-entering/is-leaving CSS classes, wire filter buttons to update state.filter and re-render with aria-pressed; keep all state in single in-module state object. `[test: npx playwright test tests/e2e/create-app-js-module.spec.ts]`
- [ ] Modify index.html markup. Update `src/index.html` — change `<title>` to "To-Do", replace `<h1>Hello World</h1>` with centered `<main data-testid="todo-app">` containing h1 heading "Todo", form with input (placeholder, autocomplete off, maxlength 240) and submit button, filter section with three button elements for all/active/completed, empty ul todo-list, empty state paragraph, footer span for count; add `<script type="module" src="app.js"></script>` at end of body while preserving existing stylesheet link tag. `[test: npx playwright test tests/e2e/modify-index-html-markup.spec.ts]`
- [ ] Modify style.css design system. Update `src/style.css` — declare CSS custom properties on :root for colors (bg, surface, border, text, muted, accent, danger), spacing variables (--space-1 through --space-6), transitions (--transition-fast 150ms, --transition-base 200ms), and font stack; apply box-sizing global reset with no margins; style body with flex center layout, main as centered card max-width 560px with shadow/rounded corners, form as flex row with growing input and accent submit button, filter buttons as pill buttons with active state styling, todo-list items with bottom borders and flex layout, .todo--completed for strikethrough, custom checkbox controls, delete button icon hidden until hover/focus-within; define @keyframes todo-enter and todo-leave animations applied via is-entering/is-leaving classes; add media queries for ≤480px (stacked form), 481–768px tuning, >768px responsive layout; add @media (prefers-reduced-motion: reduce) block disabling all transforms/opacity animations. `[test: npx playwright test tests/e2e/modify-style-css-design.spec.ts]`
